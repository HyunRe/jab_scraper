[pytest]
# Python 경로에 현재 최상위 루트 디렉토리(.)를 추가하여 app/ 모듈을 인식하게 합니다.
pythonpath = .

# pytest 실행 시 기본으로 탐색할 테스트 디렉토리를 지정합니다.
testpaths = tests

# 테스트 파일명 규칙을 설정합니다 (기본값: test_*.py 또는 *_test.py)
python_files = test_*.py